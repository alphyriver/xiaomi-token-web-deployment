{
  "name": "xiaomi-token-web-deployment",
  "version": "1.3.0",
  "license": "GPL-3.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "wrangler dev -c wrangler.workers.toml",
    "deploy": "wrangler deploy -c wrangler.workers.toml --minify",
    "build": "tsc",
    "serve": "tsx watch src/server.ts",
    "vercel-build": "echo 'No build needed'",
    "build:pages": "npm run build && rm -rf dist && mkdir -p dist && cp index.html dist/",
    "dev:pages": "npm run build:pages && wrangler pages dev"
  },
  "dependencies": {
    "hono": "^4.8.4",
    "@hono/node-server": "^1.15.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
